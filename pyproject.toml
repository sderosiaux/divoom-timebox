[project]
name = "divoom-timebox"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pillow>=10", "numpy>=1.26"]

[tool.setuptools]
packages = ["divoom"]

[tool.pytest.ini_options]
pythonpath = ["."]
